# Core web framework
flask==3.0.0
flask-socketio==5.3.5
python-socketio==5.10.0

# Audio processing
sounddevice==0.4.6
pyaudiowpatch==0.2.12.7
resampy==0.4.3
soundfile==0.12.1

# PyTorch (stable versions work best with pyannote.audio)
# NOTE: torch 2.9.1 works, torch nightly 2.10.0 has compatibility issues with torchaudio
torch==2.9.1
torchaudio==2.9.1

# Transformers and dependencies
transformers
accelerate
sentencepiece
protobuf

# Speaker diarization (3.1.1 is compatible with torch 2.9.1/torchaudio 2.9.1)
pyannote.audio==3.1.1
pyannote.core

# Utilities
numpy<2.0.0
langdetect
python-dotenv
